# Copy this to .env.local and fill values (do NOT commit secrets)
# Server-side API key used by the proxy to call the fact-check backend
FACT_CHECK_API_KEY=your_fact_check_api_key_here

# Optional: override the external API base URL (defaults to the deployed URL)
NEXT_PUBLIC_API_BASE_URL=https://r8wncu74i2.us-west-2.awsapprunner.com
